export interface Item {
    id: string;
    name: string;
    assignedTo: string;
}

export interface User {
    id: string;
    name: string;
}

export interface List {
    id: string;
    name: string;
    display_name: string;
}